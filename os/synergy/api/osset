function readLine(file,line)
  lines = {}
  for line in io.lines(file) do
    lines[#lines+1] = line
  end
  return lines
end
function read(path)
  settings = {}
  file = fs.open(path,"r")
  fileData = file.readAll()
  return settings
end
